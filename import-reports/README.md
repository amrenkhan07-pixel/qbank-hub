# QBank import reports

Every dry run and import writes a timestamped JSON audit report here. Generated
reports are intentionally ignored by Git because they may contain source content
summaries. Keep the reports with the source archive or another protected backup.
